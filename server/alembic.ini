[alembic]
script_location = alembic
prepend_sys_path = src
# DB URL is injected by alembic/env.py from APP_DATABASE_URL (never store
# credentials in this file). Keep this file ASCII-only: configparser on
# Windows decodes it with the locale codec (GBK), not UTF-8.
