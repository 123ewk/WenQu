# 对接说明(web 前端 ↔ 后端)

> 本阶段交付:Vue 3 + TypeScript + Element Plus 复刻 `docs/design/` 原型,全量对接 M1(账号体系 + 多空间管理)与 M2(知识库 / 入库 / 检索 / 流式问答);仅 API Key 页占位。
> 接口契约唯一事实来源:**后端 `main` 的 `docs/api/openapi.json`**(当前 **31 路径 / 46 方法**,清单见 `web/对接标准.md` 附录)。
> ⚠️ 本 worktree(`feat/web-m1`)内那份 `docs/api/openapi.json` 是**旧副本(26 路径 / 38 方法)**,缺第二轮 8 个方法,**不要拿它核对**。
> M1 细节见 `docs/api/m1-接口说明.md`,M2 的流程与顺序约束见 `docs/api/m2-接口说明.md`;规则与达标判据见 `web/对接标准.md`。

## 1. 环境变量

`web/.env.development`:

```
VITE_API_BASE_URL=/api/v1
```

后端未配置 CORS,开发环境**必须**走 Vite 代理(`web/vite.config.ts`):`/api → http://localhost:8000`。禁止前端直连跨域。

## 2. 启动方式

```bash
# 依赖与后端(仓库根目录)
make compose-dev-up        # postgres + minio
make db-migrate            # 迁移
make parser-run            # 文档解析服务(:50051),worker 依赖它
make worker-run            # 入库流水线 worker(不起则文档永远停在 pending)
make server-run            # 主服务 http://localhost:8000,/docs 有 Swagger

# 前端
cd web
npm install
npm run dev                # http://localhost:5173
npm run build              # vue-tsc 类型检查 + 构建,必须零错误
```

**Windows 端口提示**:若 50051 落在系统保留段(`netsh interface ipv4 show excludedportrange protocol=tcp` 可查),
启动 parser 时换端口 `cd parser && uv run python -m parser_service.server --addr 127.0.0.1:50071`,
并用环境变量覆盖后端配置(不必改 `server/.env`):

```bash
cd server && PARSER_GRPC_ADDR=127.0.0.1:50071 uv run uvicorn app.main:app --port 8000
cd server && PARSER_GRPC_ADDR=127.0.0.1:50071 uv run python -m app.worker
```

**真实 Key**:向量化需 `DASHSCOPE_API_KEY`,问答需 `DEEPSEEK_API_KEY`;未配置时上传会进死信、问答流回 `error` 事件。

## 3. Token 存储键名(localStorage)

| 键 | 内容 |
|---|---|
| `wenqu_access_token` | 访问令牌(请求拦截器自动附带 `Authorization: Bearer`) |
| `wenqu_refresh_token` | 刷新令牌(**一次性**,每次 refresh/switch-space 后立即用新值覆盖) |
| `wenqu_user` | UserOut JSON(F5 恢复登录态) |
| `wenqu_spaces` | SpaceBriefOut[] JSON(含各空间 role) |
| `wenqu_current_space_id` | ⚠️ **已被契约取代,待移除**:后端已提供 `AuthResponse.current_space_id` / `UserOut.current_space_id`,应以契约值为准并删除此自维护键,否则会出现"前端以为在 A 空间、token 实际在 B"的错位(见 `契约补充对接说明.md` §2 与 `对接缺口清单.md` §三.3) |
| `wenqu_sidebar_collapsed` | 侧边栏折叠状态 |

## 4. 认证链路(实现于 `src/api/http.ts` + `src/stores/auth.ts`)

1. 请求拦截器统一附带 Bearer access_token;
2. 收到 401 且 `error.code ∈ {AUTH_REQUIRED, TOKEN_EXPIRED}`(或裸 401)→ 用 refresh_token 调 `/auth/refresh` → 成功后**重放原请求(仅一次)**;
3. **并发 401 防抖**:共享 `refreshAccessToken()` 的 refreshing Promise,多个请求同时过期只刷新一次,其余挂起等待后重放;
4. refresh_token 一次性:刷新/切空间成功后新值**立刻覆盖**旧存储值;
5. 刷新失败(`REFRESH_TOKEN_INVALID`)→ 清空凭据 → 跳登录页(带 `redirect`);网络错误不清凭据;
6. 切换空间:响应是完整 AuthResponse,前端**整体替换** user/spaces/双令牌;令牌轮换类操作在 Store 内**串行执行**,避免并发使用已作废的 refresh_token(且内部函数不叠加串行,防嵌套死锁);
7. 改密成功(204)→ 后端吊销全部会话 → 前端清凭据并强制回登录页;
8. `/auth/login|register|refresh|switch-space|logout` 自身的 401(如密码错误)不触发刷新链;
9. F5:路由守卫检查登录态;`AppLayout` 挂载时调 `GET /users/me` + `GET /spaces` 静默校验并刷新。

> **活动空间**:后端已提供 `AuthResponse.current_space_id`(登录/注册为 `null`,`switch-space` 后为新空间,`refresh` 延续原绑定)。
> 待接入后应改为以契约值为准;注意 `refresh` 响应也带该值,`session.saveAuth()` 需一并持久化。

## 5. M2 对接要点

### 流式问答(SSE,`src/api/chat.ts`)

- 用 `fetch` + `ReadableStream` 读取(**不能用 EventSource**:它发不了 POST 请求体与 Authorization 头);
- 五种事件按 `type` 分派:`meta`(记录新会话 id 并同步路由/侧栏)、`citations`、`delta`(**累加**正文)、`done`(`cited_indexes` 决定抽屉展示哪几条)、`error`(展示错误、停 loading,**HTTP 仍 200**);
- ⚠️ `error` 事件的 `code` **可选**:检索失败带 `code`,检索兜底与模型调用失败只有人话 `message`(源码 `server/src/app/application/service/qa.py:153/157/199`);且**失败路径不发 `done`** —— 收尾时按 `done` / `error` / 都没有 三种情况分别处理,**不要拿 `done` 当唯一结束信号**;
- 校验类错误(401/404/422)发生在流开始前,能正常拿到状态码;流内 401 复用同一套一次性 refresh 轮换后重发一次;
- 引用角标:正文里的 `[n]` 渲染为可点击角标,点击打开 380px 引用抽屉,展示 `excerpt` 与 `filename/breadcrumb/page`;抽屉的"查看完整原文块"调 `GET .../chunks/{chunk_id}` 取全文(第二轮已提供,前端待接入);
- 停止生成:`AbortController` 中断流,已生成内容保留;刷新页面后由 `GET /conversations/{cid}/messages` 恢复历史(assistant 消息自带 citations,角标与抽屉可直接复现);
- **断线/中途结束**:SSE 已有心跳帧(`OPT-2` 已交付),前端解析跳过非 `data:` 行即可;断线时后端会保存部分答案(`OPT-1` 已交付),
  前端在静默截断时先重拉一次历史取回,取不到再给"连接中断"提示;续流未实现(`OPT-3`,M4),一律回退重新拉取。

### 上传与入库状态机(Tab A)

- `POST .../documents` 只负责**存文件与入队**,返回 `status=pending`;
- 前端轮询 `GET .../documents`(2s,页面不可见时暂停)刷新状态;全部终端态后自动停止轮询;
- 状态展示:排队 → 解析 → 分块 → 向量化 → 完成;`failed` 行可展开查看原因(`error_code=INGEST_FAILED`,文案按接口文档约定);
- 上传按钮走统一 axios 通道(带 401 链),进度用 `onUploadProgress` 反映在顶栏上传浮层;入队后的解析进度在文档列表查看;
- 旧版 `.doc/.xls/.ppt` 不支持(`UNSUPPORTED_FORMAT`),提示另存为新格式。

### 检索测试(Tab B)

- `POST /retrieval/search` 展示 RRF 融合分与 `vector_rank`/`fulltext_rank` 双路命中排名;仅检索当前知识库。

## 6. 角色显隐(体验层,后端 403/404 兜底)

角色数值:Viewer=10 / Editor=20 / Admin=30 / Owner=40,来源 `SpaceOut.role` / `spaces[].role`。

| 能力 | 门槛 |
|---|---|
| 查看知识库 / 文档 / 分块 / 检索测试 | 任意成员 |
| 新建知识库、改名改描述、上传/删除文档 | Editor(20)+ |
| 删除知识库 | Admin(30)+ |
| 邀请成员、改角色、移除成员、空间设置、审计日志 | Admin(30)+ |
| 删除空间 / 转让所有权 | Owner(40) |
| 退出空间 | 任意成员(Owner 不可,先转让或删空间) |

`FORBIDDEN` → 提示无权限;`SPACE_NOT_FOUND` → 引导回工作台。

## 7. 目录结构

```
web/src/
  api/        # http.ts(客户端+401链) session.ts(持久化) auth/users/spaces/knowledge/retrieval/chat.ts types.ts
  stores/     # Pinia:auth.ts(登录态/空间/角色) uploads.ts(全局上传进度) ui.ts
  router/     # 路由 + 守卫
  layouts/    # AppLayout(侧边栏 240 + 顶栏 56)
  components/ # AppSidebar / CreateSpaceDialog / KbFormDialog / UploadProgress
  views/      # login/ chat/ kb/(列表·详情·分块抽屉) space/ audit/ profile/ placeholder/
  utils/      # roles.ts ingest.ts(状态机) markdown.ts(安全渲染) format.ts
  styles/     # main.css(设计令牌 → Element Plus CSS 变量)
```

## 8. 与原型的刻意差异

- Element Plus 组件按设计令牌换肤(主色 #4F6EF2、边框 #E5E7EB、圆角 8/12px、表格行高 44px);
- 原型演示用的"原型状态切换"浮条、假数据列表全部移除;
- 契约不支持的能力未渲染假交互(本轮第二轮已补齐接口的项见 §9,接入前仍维持占位/降级):知识库卡片文档/分块统计、嵌入模型选择与重建索引、**检索测试 Tab 的向量权重/阈值/Rerank**、文档重新解析、会话重命名。逐条依据见 `对接缺口清单.md`。

---

## 9. 后端已补齐、待前端接入(第一轮 + 第二轮)

后端已补齐第一轮缺口 #1/#3/#4/#5/#6/#7/#9(`b329c16`、`ec5910b`),并在第二轮补齐知识库侧全部九项 + 真正闭合 #8(`92a1c5d`,迁移 `0009`)。**本节为待接入状态,尚未落地到代码**。

### 接入前置条件

1. **契约必须取自后端 `main`**:本 worktree 内那份是旧副本(26/38),缺第二轮 8 个方法;运行中的 `:8000` 也可能是旧构建 —— 需先同步契约、重启服务并执行迁移到 `0009`;
2. 第二轮文档 §9 的 10 条自检清单可作为接入后的验收依据。

### 接入要点(细节与证据见 `对接缺口清单.md` §二/§三)

| 能力 | 关键做法 |
|---|---|
| 活动空间 `current_space_id` | 改为契约优先,移除 `wenqu_current_space_id`;`refresh` 也会带回该值,`saveAuth()` 要一起持久化 |
| 上次登录 `last_login_at` / `last_login_ip` | IP 尚未解析 `X-Forwarded-For`(`OPT-12`),挂网关时会显示代理地址,文案用中性的「上次登录来源」 |
| 头像三端点 | 读取**必须带 Authorization**,取 blob + `createObjectURL` 并 `revokeObjectURL`;`avatar_url` 现为 `/users/me/avatar`(不带 `/api/v1` 前缀),可直接喂给 baseURL 为 `/api/v1` 的客户端 |
| 空间检索参数 `retrieval_params` | Admin+ 可改,真实驱动检索;`PATCH` 时 `name` 必填、`description` 省略会被**重置为空串**,须一并提交;做"两权重之和 ≤ 1"的前端校验 |
| 审计筛选/结果/操作人 | 动作下拉从 `AuditAction`(**18** 值)生成;`since`/`until` 为闭区间,`until` 取当天 `23:59:59.999`;可直接用 `result` 渲染成功/拒绝,并改用 `actor_name` 弃掉成员映射 |
| 模型清单 `GET /models` | 提交 `id` 字段;换 embedding 模型会改向量维度,需二次确认并提示可用 `reparse` 重建 |
| 重新解析 `POST .../reparse` | 仅终结态(`completed`/`failed`)可调,处理中返回 409 `DOCUMENT_BUSY`;成功后回到 `pending`,须恢复该行轮询 |
| 单块全文 `GET .../chunks/{chunk_id}` | 引用抽屉"查看完整原文块"的数据源(不再只有 `excerpt`);文档删除后 404 属预期,抽屉内限高滚动 |
| 顶栏入库进度 `GET .../ingestion-progress` | `total_active > 0` 时轮询、归零停止;`has_failure` 时提示;已完成项不返回(需与浏览器上传进度区分) |
| 知识库聚合统计 | 卡片四统计 + `index_status` 四态,注意优先级 `empty`→`processing`→`degraded`→`ready`(在途优先于失败);`size_bytes` 前端自行格式化 |
| 文档失败原因 | 失败行显示 `error_message`(任务侧真实原因,可能偏运维向)+ "重新解析"按钮 |
| 检索测试调参 | `SearchRequest` 四个请求级覆盖参数只对本次生效、不写回空间配置;`vector_rank`/`fulltext_rank` 可能为 `null`;`score` 是 RRF 融合分,不是相似度、不可跨查询比较 |
| 会话重命名 | `PATCH .../conversations/{cid}`,`title` 1–128 字;列表按 `updated_at` 倒序 |
| 前端自查修正 | `AuditView.vue` 的 `ACTION_LABELS` 缺 `access.denied`、`document.reparsed`(后端现为 18 项),需补齐标签 |

### 仍缺、不在此范围

审计按昵称搜索(只有 `actor_id`)、他人头像(`OPT-19`)、入库各阶段耗时(`OPT-9`)、`meta.kind` 细分"标题"、
SSE 续流(`OPT-3`)。详见 `对接缺口清单.md` §1.3。

---

## 10. M3 第一轮已对接(本轮完成)

后端第一批 6 项见 `docs/api/m3-第一轮对接说明.md`。前端本轮落地并实测:

### 10.1 API Key 页(07,原占位页已删除)

- 新增 `src/api/apiKeys.ts` + `src/views/apiKeys/ApiKeysView.vue`,路由 `api-keys` 由占位改为真实页面;
- 类型:`ApiKeyOut` / `ApiKeyCreatedOut`(`plaintext` 仅创建响应有)/ `CreateApiKeyRequest`;
- 页面要点:列表含**已吊销**项并置灰、能力徽章、`kb_ids` 空 → "全部知识库"、`last_used_at` 空 → "从未使用";
  新建为**两步弹窗**(表单 → 一次性明文展示,深色代码块 + 复制 + 红色警示);吊销有二次确认并写明"立即 401、不可恢复";
  权限:Editor+ 才渲染"新建/吊销",Viewer 显示角色提示;
- ⚠️ **能力/KB 多选用 `el-checkbox-group`**:独立的 `el-checkbox` 只接受布尔值,数组必须包在 group 里,否则类型报错。
  注意 Element Plus 给 `.el-checkbox-group` 设了 `font-size:0 / line-height:0`(为消除勾选项间空白),
  **自定义文案放进组内会继承这两个 0**——`line-height` 计算值是长度会一并继承为 0,行盒塌成 0 导致多行文字重叠;
  需在自己的容器上重置 `font-size`/`line-height`。

### 10.5 Element Plus 的两个定位/继承陷阱(本轮踩到并修复)

- **`el-dropdown` 的包装元素是零尺寸的 `position: relative`**:把 `position: absolute` 加在触发器按钮上时,
  包含块是那个包装元素(按钮被抽离后其宽高为 0),元素会跑到容器外的左下角。
  正确做法是把 `absolute/top/right` 加在 **`el-dropdown` 本身**(如 `KbListView` 的 `.kb-more-wrap`),按钮回归常规流。
- **flex 子项默认 `min-width:auto` 不会收缩**:卡片底部的模型徽章在窄容器下不换行/不省略,而是撑破父容器。
  需给徽章 `min-width:0`,并在内层文本上用 `.truncate` 出省略号;把短信息(维度)单独设为 `flex-shrink:0` 保留可读性。

### 10.2 对话页(SSE)

- **心跳帧**:`: ping` 是不以 `data: ` 开头的注释行,现有解析已跳过(实测确认),无需改动;
- **流内错误**:`error` 事件的 `code` **可选**(检索侧带、兜底侧只有 `message`),按 code 分支前先判空;
  且**失败路径不发 `done`** —— 收尾按 `done` / `error` / 都没有三种情况分别处理;
- **修了一个真实缺陷**:首次提问会 `router.replace` 到新会话,路由变化触发历史重拉,而后端在失败/中断路径
  **不存助手消息**,导致重拉把本地渲染的错误提示冲掉,只剩孤立提问。现对刚流式结束的会话**跳过一次重拉**(`skipReloadId`),
  并新增"连接中断、本轮无内容"的兜底提示;静默截断时先尝试重拉后端保存的部分答案(`OPT-1` 的后端能力)。

### 10.3 审计动作标签(23 值)

`ACTION_LABELS` 已按**后端源码**补齐至 23 个动作值(含 `user.updated`/`user.avatar_uploaded`/`user.avatar_deleted`
/`document.reparsed`/`api_key.created`/`api_key.revoked`/`access.denied`)。
⚠️ 契约 `AuditAction` 只导出 20 个(缺 3 个 `user.*`),以源码为准,待后端重导契约。

### 10.4 顺带修掉的开发环境缺陷

`vite.config.ts` 的代理键原为 `'/api'`(前缀匹配),会把前端路由 **`/api-keys`** 也代理到后端,
直接打开或刷新该页会拿到后端的 404 JSON 而不是 SPA。现改为 `'^/api/'`,只代理真正的接口路径。
